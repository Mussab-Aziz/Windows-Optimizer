import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Static export is only used for production builds
  // For dev mode, we keep it server-rendered so client JS works properly
};

export default nextConfig;
